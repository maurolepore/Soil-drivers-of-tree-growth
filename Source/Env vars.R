# Main path variables used for the paper:
# Zemunik et al. (2018) Soil drivers of local-scale tree growth in a lowland tropical forest


mainSourcePath <- paste( baseRSourcePath, "Source/", sep="" )
dataPath <- paste( baseRSourcePath, "Data/", sep="" )
baseOutPath <- paste( baseRSourcePath, "Graphs/", sep="" )
