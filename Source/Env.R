# Script to set up the general environment associated with the analyses for the
# paper: Zemunik et al. (2018) Soil drivers of local-scale tree growth in a lowland tropical forest

source( paste( baseRSourcePath, "Source/Env vars.R", sep="" ) )

source( paste( mainSourcePath, "packages.R", sep="" ) )

PathsAndEnvironmentInitialised <- T